my @comondos;
my $comondsiz;
my $comnom;

@comondos = @ARGV;
$comondsiz = @comondos;
if ( $comondsiz < 0.5 )
{
  die "\nAt very least, a specific action must be named.\n\n";
}
$comnom = shift(@comondos);

$ENV{"SCRIP_DIRECTORY"} = $scripdir;
exec("perl",$scripdir . "/" . $comnom . ".pl", @comondos);

